/**
 * ODIN
 * APIs fetchs the data based on query
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IPExploitDetails from './IPExploitDetails';

/**
 * The IPCVE model module.
 * @module model/IPCVE
 * @version 1.0
 */
class IPCVE {
    /**
     * Constructs a new <code>IPCVE</code>.
     * @alias module:model/IPCVE
     */
    constructor() { 
        
        IPCVE.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>IPCVE</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IPCVE} obj Optional instance to populate.
     * @return {module:model/IPCVE} The populated <code>IPCVE</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IPCVE();

            if (data.hasOwnProperty('exploit')) {
                obj['exploit'] = ApiClient.convertToType(data['exploit'], [IPExploitDetails]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('references')) {
                obj['references'] = ApiClient.convertToType(data['references'], ['String']);
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'Number');
            }
            if (data.hasOwnProperty('services')) {
                obj['services'] = ApiClient.convertToType(data['services'], ['String']);
            }
            if (data.hasOwnProperty('severity')) {
                obj['severity'] = ApiClient.convertToType(data['severity'], 'String');
            }
            if (data.hasOwnProperty('summary')) {
                obj['summary'] = ApiClient.convertToType(data['summary'], 'String');
            }
            if (data.hasOwnProperty('vector_string')) {
                obj['vector_string'] = ApiClient.convertToType(data['vector_string'], 'String');
            }
            if (data.hasOwnProperty('weakness')) {
                obj['weakness'] = ApiClient.convertToType(data['weakness'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IPCVE</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IPCVE</code>.
     */
    static validateJSON(data) {
        if (data['exploit']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['exploit'])) {
                throw new Error("Expected the field `exploit` to be an array in the JSON data but got " + data['exploit']);
            }
            // validate the optional field `exploit` (array)
            for (const item of data['exploit']) {
                IPExploitDetails.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['references'])) {
            throw new Error("Expected the field `references` to be an array in the JSON data but got " + data['references']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['services'])) {
            throw new Error("Expected the field `services` to be an array in the JSON data but got " + data['services']);
        }
        // ensure the json data is a string
        if (data['severity'] && !(typeof data['severity'] === 'string' || data['severity'] instanceof String)) {
            throw new Error("Expected the field `severity` to be a primitive type in the JSON string but got " + data['severity']);
        }
        // ensure the json data is a string
        if (data['summary'] && !(typeof data['summary'] === 'string' || data['summary'] instanceof String)) {
            throw new Error("Expected the field `summary` to be a primitive type in the JSON string but got " + data['summary']);
        }
        // ensure the json data is a string
        if (data['vector_string'] && !(typeof data['vector_string'] === 'string' || data['vector_string'] instanceof String)) {
            throw new Error("Expected the field `vector_string` to be a primitive type in the JSON string but got " + data['vector_string']);
        }
        // ensure the json data is a string
        if (data['weakness'] && !(typeof data['weakness'] === 'string' || data['weakness'] instanceof String)) {
            throw new Error("Expected the field `weakness` to be a primitive type in the JSON string but got " + data['weakness']);
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/IPExploitDetails>} exploit
 */
IPCVE.prototype['exploit'] = undefined;

/**
 * @member {String} id
 */
IPCVE.prototype['id'] = undefined;

/**
 * @member {Array.<String>} references
 */
IPCVE.prototype['references'] = undefined;

/**
 * @member {Number} score
 */
IPCVE.prototype['score'] = undefined;

/**
 * @member {Array.<String>} services
 */
IPCVE.prototype['services'] = undefined;

/**
 * @member {String} severity
 */
IPCVE.prototype['severity'] = undefined;

/**
 * @member {String} summary
 */
IPCVE.prototype['summary'] = undefined;

/**
 * @member {String} vector_string
 */
IPCVE.prototype['vector_string'] = undefined;

/**
 * @member {String} weakness
 */
IPCVE.prototype['weakness'] = undefined;






export default IPCVE;

